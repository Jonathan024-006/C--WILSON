class Ninno : Persona  //2 // crea una clase llamada Ninno que hereda de la clase Persona
{
    public Ninno(string nombre, int edad) : base(nombre, edad) // define un constructor público que toma dos parámetros: un string llamado nombre y un entero llamado edad, y llama al constructor de la clase base (Persona) con esos parámetros
    { 
    }
}